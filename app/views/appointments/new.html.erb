<div class="mx-auto md:w-2/3 mt-8 w-full">
  <h1 class="font-bold text-4xl">New appointment</h1>

  <%= render "form", appointment: @appointment %>

  <%= link_to "Back to appointments", profile_path(@current_user.id), class: "mb-4 ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
